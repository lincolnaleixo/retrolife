# Security

Report vulnerabilities privately using GitHub's private vulnerability reporting for this repository. Do not put credentials, private game paths, saves, or sensitive logs in public issues.

RetroLife runs a bundled, pinned native emulation core. Game files are untrusted input; the native core is not a security sandbox. Release signing is isolated from pull-request CI. Security fixes receive a new immutable release version.
